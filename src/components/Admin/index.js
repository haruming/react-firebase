import React from 'react';
const AdminPage = () => (
  <div>
    <h1>Admin</h1>
    <p>
      Restricted area! Only users with the admin role are authorized to see this page.
    </p>
  </div>
);

export default AdminPage;